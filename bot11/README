




問題1: with F14111176 對戰時發現在50步大後期，我無腦亂吃導致給了敵方跳板，把我滅族
因為minimax的中止條件是depth=0殊不知在depth=2(換對手)時我就已經要被吃掉了。

因此在第50步時，實際上我卡在depth2到時間截止，最後輸出的是depth1 的最優解
抑或是我的minimax模擬到太深，可以把對方高價值目標滅族，導致depth2的被滅族被忽視了
並且耗費大量算力


解法: minimax中多加上終止條件
// 在 if (depth == 0) 之前加入：
int checkStatus = fastCheckGameOver(b, myColor); // 輕量級的函數，只掃描三種棋子數量
if (checkStatus == 1) return 1000000 + depth;    // 我方贏了
if (checkStatus == -1) return -1000000 - depth;  // 我方輸了（被滅族）

======================




問題2: with E24145185 (i am white) 在第10步我場面只剩2個tzaar 然而我還是選擇猛吃，導致下一回合我被滅族  
note: horizontal Effect
推測是因為miniax 的depth2都還沒跑完，導致我沒看到被滅族的結果。
最後只score只是來自於depth1，(沒什麼參考性)

解法:
x[治標:evaluate中加入<=1就給予超低分，使dept1也能做出判斷]
v[治本: 強化剪枝效率，針對==moveorder==下手，讓AI能更快模擬到depth2的被滅族結果。]  


1. Old：高度導向型 (Height-Based)
核心邏輯： 單純以 「棋塔高度 (Level)」 作為排序依據。
優點： 邏輯簡單，運算開銷極小，能確保 AI 優先爭奪制空權。
缺點： * 戰略盲區： 完全無視棋子剩餘數量。
自殺傾向： 可能為了增加高度而壓死自己最後一顆關鍵棋子。
剪枝效率低： 走法評分差距小，Alpha-Beta 難以快速剪掉無用分支，導致前期容易 Timeout。


2. New：生存與勝負導向型 (Survival & Win-State)
核心邏輯： 引入 「棋種計數器 (Piece Counting)」，將勝負條件直接融入排序。
關鍵優化：
進攻端： 優先嘗試能造成對方「滅族」或「瀕危」的走法（+20000$分）。
防守端 ： 增加「安全鎖」，嚴禁壓死自己數量 <=2 的稀缺棋子（-20000 分）。
避險意識： 優先移動自己處於危險數量的棋子去進行堆疊加固。
優點： * 加速剪枝： 透過巨大的分差（+/- 20000），迫使 Alpha-Beta 最優先處理「勝負手」，大幅提升搜尋深度。
穩定性： 解決了在 Depth 1 視角下容易忽略之;被「一回合雙殺」或「自我滅族」的問題。

=======================




注意事項
#include <bits/stdc++.h>是一個巨大懶人包
要額外加這些:
#include <iostream>  // 雖然這段沒直接用到，但通常建議包含以備輸出除錯
#include <fstream>   // 為了 ifstream
#include <string>    // 為了 string, getline, stoi
#include <sstream>   // 為了 stringstream
程式碼中使用了 [stoi]，這是 C++11 標準才加入的。
如果你的目標平台環境非常舊（例如某些老舊伺服器的 GCC 版本），這段程式碼會編譯失敗。
解法： 確保在編譯指令中加入 -std=c++11（或更高版本）。


